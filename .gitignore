# Cloudflare Workers (kept local only, deployed via Wrangler)
workers/

# Environment / secrets
.env
.env.local
*.env

# OS files
.DS_Store
Thumbs.db

# Logs and error logs
error_log
*.log

# Audio is uploaded via cPanel, never through git (times out the FTP deploy)
*.mp3
*.m4a
